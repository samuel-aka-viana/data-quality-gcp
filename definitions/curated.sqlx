config {
  type: "view",
  schema: "workshop_gov",
  name: "transactions_curated",
  tags: ["daily"]
}

SELECT
  transaction_id,
  transaction_date,
  store_id,
  product_sku,
  amount,
  customer_email
FROM ${ref("raw_transactions")}
WHERE amount > 0
